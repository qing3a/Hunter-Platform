# 3 个待办任务的提示词与计划

> **目的**：把以下 3 个任务交给其他 AI agent 独立执行。每个任务自带：
> - 可粘贴的提示词（copy-paste ready）
> - 背景与现状
> - 关键文件清单
> - 接受标准
> - 验证命令
>
> **执行方**：`docs/superpowers/plans/2026-07-11-session-multirole-progress-2.md` 已经记录了 R1.C2 完成状态。下面 3 个任务是 R1 的剩余 P0/P1 + 1 个长期 open issue。
>
> **基线**：`origin/main = 2bb0f7d`（2026-07-11）。

---

## 任务 A：修 vitest worker crash

### 背景

`docs/issues/2026-06-20-vitest-worker-crash.md` 已经记录。本人 session 也多次遇到：跑全量测试时 `Worker exited unexpectedly` 导致 `Test Files` 比真实少 1-5。

**已知绕过**：`vitest.config.ts` 里把 `pool: 'forks'` + `poolOptions.forks.singleFork: true` 已经是当前配置 — 但崩溃仍发生。说明即使 singleFork 也不完全解决问题（或者绕过方案写错位置）。

**当前 `vitest.config.ts`**：
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
    globalSetup: ['./tests/global-setup.ts'],
  },
});
```

### 复制粘贴的提示词

```
修复 D:/dev/hunter-platform 上 vitest 在 Windows (MINGW64) 上的 "Worker exited unexpectedly" 崩溃。
该问题在 docs/issues/2026-06-20-vitest-worker-crash.md 有完整描述。

## 必读
- docs/issues/2026-06-20-vitest-worker-crash.md（issue 全文，含已排除的可能性、临时绕过、根因推测）
- vitest.config.ts（当前 singleFork 配置；需要理解为什么没解决问题）
- pnpm-lock.yaml（确认 vitest/tinypool 版本：vitest@2.1.9, tinypool@1.1.1）
- tests/global-setup.ts（全局 setup 文件，可能与 worker 关闭时序冲突）

## 当前现象
即使 singleFork=true，跑全量测试 (pnpm test) 时仍偶发崩溃：
- Test Files 显示 (N-1) 或 (N-2)，有 "1 error: Worker exited unexpectedly"
- 单独跑任何文件 100% 通过
- 5 次跑 5 次结果可能不同

## 目标
1. 找到根因（最可能是 tinypool 在 Windows 上的某个边界 bug，或者测试 cleanup 代码在 worker fork 时撞上）
2. 提出最小修复（config tweak / 代码调整 / 依赖升级 三选一）
3. 验证：连续跑 5 次 `pnpm test`，每次都显示 `Test Files N passed (N)` 且 0 errors
4. 在 docs/issues/2026-07-11-vitest-worker-crash-resolved.md 写一份解决报告（或者更新原 issue 加 RESOLVED 标记）

## 接受标准
- 5/5 次运行无崩溃
- 所有测试仍然通过（baseline = 472 个测试全绿）
- typecheck exit 0
- 没有引入新的依赖除非必要
- 提交并推送到 origin/main

## 验证命令
```bash
cd D:/dev/hunter-platform
pnpm typecheck
for i in 1 2 3 4 5; do
  echo "=== Run $i ==="
  pnpm test 2>&1 | tail -5
done
```

## 提示（不必走完所有路径）
- 优先：vitest 配置 + cleanup 代码排查（cheap）
- 次优：升级 vitest 到 2.1.x 最新 patch 或 3.x（中等成本，可能引入回归）
- 备选：换 worker pool（如 threads 而非 forks）
- 不要：直接禁掉所有 cleanup code（可能掩盖真实 leak）
```

---

## 任务 B：C3 — Webhook inbox dedup + `POST /v1/webhooks/qing3`

### 背景

`docs/superpowers/specs/2026-07-11-positioning.md` 第 98-99 行提到：
> ow-recruit ... Optionally connects to hunter-platform via webhook (`POST /v1/webhooks/qing3` planned C3)
> Implements the exact webhook contract we documented (X-Hunter-Signature, ±5min replay, body-hash dedup)

**当前状态：**
- ✅ 出站 webhook 已实现：`webhook_delivery_queue` 表 (v002)、`src/main/modules/webhook/worker.ts` 后台投递、HMAC 签名 (`hmac.ts`)
- ❌ 入站 webhook endpoint 不存在
- ❌ dedup 机制（按 body-hash）不存在

### 复制粘贴的提示词

```
在 D:/dev/hunter-platform 实现入站 webhook endpoint 与 body-hash dedup（positioning R1.C3）。

## 必读
- docs/superpowers/specs/2026-07-11-positioning.md（§4.1 ow-recruit 集成、第 146 行 C3 描述）
- docs/superpowers/specs/2026-07-01-product-positioning-standard.md（如有 webhook 契约说明）
- src/main/modules/webhook/hmac.ts（现有 HMAC 签名实现，复用 verify 函数）
- src/main/db/repositories/webhook-delivery-queue.ts（现有出站队列模式参考）
- src/main/routes/（新增入站 router 文件的标准结构）

## 任务范围

### 1. 数据库 schema（新增 v032 migration）
新建 `src/main/db/migrations/v032_webhook_inbox.sql`：
- 表 `webhook_inbox_deliveries`：
  - `id` (TEXT PK, ULID/UUID)
  - `endpoint` (TEXT) — 哪个端点收的（'qing3' / 'generic' 等）
  - `sender_id` (TEXT NULL) — 来源标识（可选）
  - `body_hash` (TEXT NOT NULL) — SHA256(body) — **UNIQUE(endpoint, body_hash)** 防重复
  - `headers_json` (TEXT)
  - `body_json` (TEXT)
  - `received_at` (TEXT)
  - `processed_at` (TEXT NULL)
  - `status` (TEXT) — 'pending' | 'processed' | 'duplicate' | 'failed'
  - `error` (TEXT NULL)
- 在 `migrations.ts` 注册 version 25
- 修复 v029 CHECK 约束（如果还存在 `'pm', 'pm'` 重复项 — commit 55309e6 提到过但未实际修复）

### 2. 入站 endpoint
新增 `src/main/routes/webhooks-inbox.ts`：
- `POST /v1/webhooks/qing3`
- 验证 HMAC 头（`X-Hunter-Signature`）— ±5 分钟时间戳窗
- 计算 body SHA256
- INSERT INTO webhook_inbox_deliveries ... — 利用 UNIQUE 约束触发 dedup
- 处理对应事件（先打印 'processed'，TBD 后实际处理；占位即可）
- 返回 200 + `{ ok: true, deduped: bool }`
- 在 `src/main/server.ts` 挂载到 `/v1/webhooks`

### 3. 仓库层
新建 `src/main/db/repositories/webhook-inbox.ts`：
- `insert({...})` → 捕获 UNIQUE 冲突返回 `{ deduped: true, id: existing_id }`
- `markProcessed(id, status, error?)`

### 4. 测试
新建 `tests/integration/webhook-inbox.test.ts`（至少 6 用例）：
- happy path: POST → 200 + deduped=false
- 重复 POST 相同 body → 200 + deduped=true（不重复处理）
- 错 HMAC → 401
- 过期时间戳（>5min）→ 401
- 缺 body → 400
- 入站后查 repo 能看到记录

### 5. docs
- `docs/superpowers/skill.md` §2.1 endpoint 表加 `POST /v1/webhooks/qing3` 一行
- 创建 `docs/superpowers/specs/2026-07-11-webhook-inbox-spec.md`（或更新 positioning spec）

## 接受标准
- `pnpm typecheck` exit 0
- 所有现有测试仍 pass
- 新 webhook-inbox 测试 6/6 pass
- `pnpm openapi:check` 无 dangling paths（更新 openapi.json）
- 提交 1-2 个 commit + 推 origin/main
```

---

## 任务 C：C4 — Skill naming alignment

### 背景

`docs/superpowers/specs/2026-07-11-positioning.md` 第 147 行：
> C4. Skill naming alignment (3 ow-recruit skills: `advance-candidate`, `send-message`, `sync-project-to-erp`)

ow-recruit 期望的 3 个 skill 名 vs 当前 hunter-platform capability 名是否一致？

**当前 capabilities：**
- `src/main/capabilities/headhunter.ts` — e.g. `headhunter.upload_candidate`, `headhunter.recommend_candidate`
- `src/main/capabilities/candidate.ts` — `candidate.respond_unlock_request`, ...
- `src/main/capabilities/employer.ts` — `employer.express_interest`, ...
- 等等

### 复制粘贴的提示词

```
在 D:/dev/hunter-platform 实现 C4（positioning R1）：让 ow-recruit 期望的 3 个 skill 名能映射到现有 capabilities。

## 必读
- docs/superpowers/specs/2026-07-11-positioning.md（§4.1 + C4 行）
- src/main/capabilities/（所有 9 个 capability set 文件）
- src/main/capabilities/index.ts（findCapabilityByEndpoint / aggregate）
- docs/superpowers/skill.md（agent 接入文档）

## 任务

### 1. 调研
读所有 9 个 capability set 文件，列出当前所有 capability 名（约 70+ 个）。然后分析 ow-recruit 期望的 3 个 skill：

| ow-recruit skill | 推测对应 hunter-platform capability | 推测 endpoint |
|---|---|---|
| `advance-candidate` | ? | ? |
| `send-message` | ? | ? |
| `sync-project-to-erp` | ? | ? |

把分析写到 `docs/superpowers/specs/2026-07-11-skill-mapping.md`。

### 2. 决策
三种可能：
- A. 现有 capability 名已经匹配 → 只需文档化映射
- B. 现有 capability 名不匹配但功能等价 → 添加 alias（在 capabilities/types.ts 加 alias 字段 + index.ts lookup 支持）
- C. 现有 capability 名完全无关 → 新增 capability + endpoint

按 C3 (task B) 的 webhook inbox 思路，ow-recruit 可能通过 `/v1/webhooks/qing3` 收到我们推送的事件，再用 skill 名 reply。所以 skill 名主要是"我们 webhook 推送的事件类型"或"ow-recruit 想用的 endpoint 名"。

### 3. 实现
基于决策 A/B/C：
- 如 A：仅文档化（无代码改动）
- 如 B：实现 alias 字段（参考 src/main/capabilities/types.ts 现有 CapabilitySet 结构）
- 如 C：新增 capability + 端点（具体设计要写在 spec）

### 4. 测试
新建 `tests/integration/skill-alignment.test.ts`：
- 验证每个 ow-recruit skill 名能被 findCapabilityByEndpoint 或 alias 解析到非 undefined
- 验证 GET /v1/capabilities 响应里能找到对应 capability

### 5. 文档
- docs/superpowers/skill.md 添加一段："ow-recruit integration" 说明 3 个 skill 的具体使用方式
- 更新 docs/superpowers/specs/2026-07-11-positioning.md C4 状态为 done

## 接受标准
- 3 个 ow-recruit skill 名 100% 能解析到具体 capability（或新能力）
- 测试通过
- typecheck exit 0
- 提交并推 origin/main
- 不要删除/重命名现有 capability（会破坏现有 ow-recruit 用户）— 只加 alias 或新 capability
```

---

## 共同提示

每个 agent 任务开始前：
1. `cd D:/dev/hunter-platform && git checkout main && git pull origin main`
2. 创建 `feature/<task-id>` 分支
3. 完成开发后用 `superpowers:finishing-a-development-branch` 技能合并 + 推送

每个任务结束应报告：
- commit SHAs
- 测试通过数（"新增 X 测试，全 Y 测试通过"）
- 任何对 git/index/R1.C2 现有行为的兼容性影响
- 是否有未完成部分（如果是，则需另开 follow-up 任务）

---

## 不要做的事（所有任务通用）

- ❌ 不要删除/破坏 R1.C2 已合并的 session + multi-role 功能
- ❌ 不要 rebase `main`（其他 agent 可能在并发操作）
- ❌ 不要引入新的 top-level dependency 除非必要（如果有，在 prompt 里说明理由）
- ❌ 不要修改 `docs/superpowers/specs/2026-07-11-session-and-multirole-design.md`（R1.C2 设计文档已锁定）
- ❌ 不要 force push