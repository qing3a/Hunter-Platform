# Release Notes — 2026-06-20 — v1.4.1 跨度合并发布

**项目**: Hunter Platform (猎头中介 API 平台)
**版本**: v1.0.2 → v1.4.1（跨度合并）
**日期**: 2026-06-20
**commits since v1.0.2**: 86 commits 在工作区累积，跨度合并为单 commit

---

## TL;DR

v1.4.1 是项目的"跨度合并发布"——一次性收口 v1.0.2 之后未提交的全部工作（v1.1 / v1.2 / v1.3 / v1.3.1 / v1.4 五个未发布版本的代码累积）和 v1.4.1 的 7 大类新功能。

**状态**：✅ 项目从"实验性 v1"演进到"API-first 生产可用"——删除 Electron 桌面客户端、严格 UTF-8 强制、限流 sliding-window + IETF 头、GDPR 软删、审计中间件、视图层、landing v2、参考 Agent。

**测试**：373 (v1.1 baseline) + 8 (v1.2 employer-talent-filter) + 6 (v1.3 market-jobs) + 4 (v1.4 openapi-coverage) = **391 / 391 PASS**。

---

## What's New

### 1. 鉴权/账户（v1.1 新增）

| 端点 | 说明 | 配额 |
|------|------|------|
| `POST /v1/auth/rotate-key` | 轮换当前用户的 API key（旧 key 24h grace） | 1 |
| `POST /v1/candidate/delete-my-data` | GDPR 软删：抹除 PII + 立即失效 API key | 1 |
| `GET /v1/users/{id}/history` | 查询本人操作历史（支持 `?limit=` `?since=` `?offset=`） | 1 |

### 2. 公开端点（v1.1 修复 + v1.3 新增）

| 端点 | 说明 |
|------|------|
| `GET /v1/market/jobs` | 猎头无需注册雇主身份即可看市场所有 open JD；可选 `industry` / `limit` / `offset` query 参数；optional auth 时扣 1 quota |
| `GET /v1/market/leaderboard` | 公开排行榜；optional auth（之前误要求 Bearer，v1.1 改为可选） |
| `GET /v1/config/{industries,title_levels,salary_bands}` | 公开配置端点；optional auth |
| `GET /v1/headhunter/candidates` | 猎头候选人列表（公开化） |
| `GET /v1/employer/talent` | 新增 `min_salary` / `max_salary` query 参数（v1.2）：与 `SALARY_BANDS` 求交集过滤 |

### 3. 限流重设计（v1.0.0）

- 算法：fixed-window → **sliding-window-counter**
- 1h 阈值上调 1.5x（candidate 200→300、headhunter 500→750、employer 800→1200）
- 响应新增 IETF `RateLimit-Limit` / `RateLimit-Remaining` / `RateLimit-Reset` 头
- 任一窗口 remaining < 20% 时新增 `RateLimit-Policy: warn` 头（软警告）
- 429 响应 `Retry-After` 始终存在；撞限后能渐进恢复
- 旁路：`RATE_LIMIT_ENABLED=false`（关闭所有限流）+ `X-RateLimit-Skip: 1`（跳过单次请求）

### 4. action_history 审计中间件（v1.1 新增）

- 路由级审计中间件（`AUDITED_PREFIXES` 改为前缀匹配，覆盖 `/v1/auth/*` 和 `/v1/users/*`）
- 完整 `action_type` 枚举映射表（`src/main/modules/audit/route-action-map.ts`）
- last-segment fallback：未匹配时用 last resource segment
- 5 步 PII 隔离：写入审计时过滤 PII 字段

### 5. 一次性 token 视图层（v1.1 新增）

- `POST /v1/views/audit/{user_id}`：审计视图（24h 过期）
- `POST /v1/views/recommendation/{rec_id}`：推荐视图（单次使用）
- `view_url` 字段在响应里返回；客户端打开后看到人类可读的 HTML 渲染

### 6. landing v2 + 公共 dashboard（v1.0.0）

- landing 页面 v2（marketplace 风格 + social proof）
- 公共 `GET /dashboard` 端点

### 7. 参考 Agent（v1.0.0）

- `examples/reference-agent/`：自动验证 skill.md 列出的 27 个端点
- 跑通 37 个端点（多角色测试自然重复）
- 用作 contract test、参考实现、CI 烟雾测试

---

## Stats

| 项 | v1.0.2 | v1.4.1 | 变化 |
|----|--------|--------|------|
| Endpoint coverage (skill.md) | 25 | **29** | +4 (v1.1 + v1.3) |
| Endpoint tested by reference agent | 0 | **37** | NEW |
| Integration tests | 177 | **391** | +214 |
| Unit tests | — | — | (总计入 391) |
| OpenAPI 自动生成 | 无 | **有**（`pnpm openapi:generate`） | NEW |
| OpenAPI 覆盖测试 | 无 | **4** | NEW |
| Spec docs | 8 | 8 | — |
| Plan docs | 16 | 16 | — |
| Release notes | 3 | **4** | +1 (this) |
| New example app | 1 | 1 | — |
| Electron 客户端 | **已删** | **已删** | v1.1 删除 |
| DB migrations | 5 | **8** | +3 (v006/v007/v008) |
| 鉴权/限流审计 | 基础 | **完整中间件** | 重设计 |
| 一次性 token 视图 | 无 | **5 种类型** | NEW |
| skill.md 章节 | 13 | **15** | +2 (OPERATIONS cross-ref + §14 Agent 决策手册) |

---

## 项目最终能力

### 三角色 API（29 endpoints）
- **Auth**: register + rotate-key
- **User**: status + history (per role)
- **Employer**: jobs, talent browse (7 filter params), recommendations, unlock, placements
- **Headhunter**: candidates upload + publish, recommendations, withdraw
- **Candidate**: opportunities, access-log, approve/reject unlock, GDPR delete, export
- **Market/Config**: leaderboard, jobs (public), industries, title_levels, salary_bands
- **Public**: health, skill.md, openapi.json, metrics
- **View layer** (render tokens): audit + recommendation views

### 业务能力
- 候选人 PII AES-256-GCM 加密 + 字段级脱敏
- 4 步解锁状态机（pending → employer_interested → candidate_approved → unlocked → placed）
- 配额 / 限流（sliding-window-counter）/ 幂等 / webhook 异步投递
- 审计日志（action_history + route-action-map 枚举）
- 一次性 token 视图（5 种类型：24h 过期 vs 单次使用）
- 公开 landing 页（marketplace 风格 + social proof）+ 公共 /dashboard

### 工程能力
- 8 个 DB migration（v001~v008）
- TypeScript strict mode + ESM
- 零外部运行时依赖（5 个）
- 完整 superpowers 流程（brainstorm → spec → plan → execute → release notes）
- Reference agent 自动验证 docs vs code
- OpenAPI 自动生成（`pnpm openapi:generate` + 4 个覆盖测试）

---

## What It Looks Like to a User

一个外部 Agent 想集成 Hunter Platform：

1. 读 `https://.../v1/skill.md`（29 endpoint 完整文档 + schema + §14 Agent 决策手册）
2. 调 `POST /v1/auth/register` 拿到 `api_key`
3. 调各种业务 endpoint（可选参考 `examples/reference-agent/` 的实现）
4. 收到响应里的 `view_url`（一次性 token）→ 浏览器打开看人类可读的 HTML
5. 业务完成

整个过程 **不需要任何 UI、任何 SDK、任何认证流程**（除了 Bearer token）。

---

## What's Next (Future v1.x)

v1.4.1 是当前可用度。下面这些可以加但**不是阻塞**：

| 项 | 价值 | 何时做 |
|----|------|-------|
| CI（GitHub Actions）| 自动跑 reference agent + tests | 任何 PR 时 |
| 部署文档（how to deploy）| 让别人能跑 | 有人要用时 |
| 真实参考 agent SDK（多语言）| 简化集成 | 有人要集成时 |
| Admin dashboard | 给运维用 | 平台 > 1000 user 时 |
| i18n | 国际化 | 用户有海外时 |
| Real-time updates（WebSocket）| dashboard 实时 | dashboard 是 ops 关键时 |
| 自动 openapi:generate 入 CI | 防 spec drift | CI 阶段 |

---

## Migration Notes

**从 v1.0.2 → v1.4.1**：跨度合并，5 个未发布版本累积的工作一次性发布。

**Breaking changes**（8 条，详见 CHANGELOG v1.4.1 主条目"⚠️ Breaking Changes"段）：
- 删除 Electron 桌面客户端
- 严格 UTF-8 请求体验收
- 限流算法 fixed-window → sliding-window
- `/v1/candidate/access_log` → `/v1/candidate/access-log`
- `delete-my-data` 路径连字符化
- `contact` 跨 role 允许
- Job 对象 `requirements` 字段从 API 表面删除
- `/v1/config/*` 和 `/v1/market/*` 改 optional auth

**给 Agent / API consumer 的建议**：

- **必做**：客户端不要再依赖 Job 对象的 `requirements` 字段（已删除）。
- **必做**：使用 `RateLimit-Remaining` 头做主动节流；收到 429 时严格按 `Retry-After` 重试。
- **建议**：调用 `/v1/config/*` 和 `/v1/market/leaderboard` 不需要 Bearer 了（optional auth）；不传也行但会扣 1 quota/天。
- **建议**：用 `POST /v1/auth/rotate-key` 定期轮换 API key（旧 key 24h 仍有效）。
- **建议**：跑 `npx tsx examples/reference-agent/src/index.ts` 验证你的集成还在工作。

---

## Contributors

- 主要工作：ZCode（主 agent）—— v1.1 / v1.2 / v1.3 / v1.3.1 / v1.4 全部 spec + plan + 执行
- 日期范围：2026-06-18 ~ 2026-06-20

---

## 这是不是 v1.4.1？

**是**——跨度合并 v1.0.2 之后未提交的全部工作，发布为单一 git commit + annotated tag v1.4.1 + GitHub Release。项目从"实验性 v1"演进到"API-first 生产可用"。

**不是** enterprise-grade——没有 CI、没有 SLA、没有 admin tools、没有 i18n。但对 v1 的"API-first headhunter platform"目标来说，跨度合并是合适的发布策略。
