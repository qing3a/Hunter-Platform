# Test artifacts. Files in this directory are auto-generated by the
# vitest conformance suite and integration tests. Each test creates
# its own DB in `tmp/conformance-<name>.db`; files here are safe to
# delete at any time.
*
!.gitignore
